src\: The ts files I coded in to make the network

dist\: The converted typescript files to javascript

build\: The appropriate peer folders with there images with the code

"npm run build": converts typescript to javascript then runs the load.js which moves the files to the approporate peer folders

"npm test": converts the test file to javascript and runs it